project:
  name: "[name]"
  created: [YYYY-MM-DD]
  release: "1.0.0"
  next_release_type: null
  active_capability: null
  active_phase: null
ideation_complete: false
arch_seeded: false
pending_arch_gap: null
pending_spec_gap: null
auto_continue: false
cwj_loop:
  max_iterations:
    ideation: 5
    spec: 3
    code: 3
narrative: null  # project-level narrative — written at ideation exit, rewritten on REQUIREMENT_DRIFT

capabilities:
  # Example entry — ideation-write-agent fills this from the capability list.
  # CAP-001:
  #   title: "Example capability"
  #   spec_done: false
  #   built: false
  #   deployed: false
  #   depends_on: []
  #   cwj_iterations:
  #     spec: 0     # incremented by orchestrator after each full spec CWJ cycle
  #     code: 0     # incremented by orchestrator after each full code CWJ cycle
  #   exit_reason:
  #     spec: null  # achieved | capped | cycling — written by orchestrator on loop exit
  #     code: null
  #   narrative: null  # single paragraph — synthesized journey; rewritten by spec-write + code-build agents after each verdict
